/**
 * 配置文件
 */
const config = {
    // eslint-disable-next-line
    serverHost: SERVER_HOST,
    // eslint-disable-next-line
    env: ENV,
    // token 名称
    tokenKey: 'JSESSIONID_my',
    userInfo: 'userinfo'
};
export default config;
